@model IEnumerable<Covid19Tracing.Models.Patients>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="card card-1">
    <div class="card-body  text-left">

        <h5><img src="/images/register.png" class="icons1" alt="" /> &nbsp; | &nbsp;Patients details</h5>
        <a asp-action="Create" class="btn btn-primary">Add new patients</a>


    </div>
</div>
<p>
    
</p>
<div class="card card-1">
    <div class="card-body">
        <table class="table" style="font-size:13px;">
            <thead>
                <tr class="bg-dark text-light">
                    <th>
                        @Html.DisplayNameFor(model => model.Full_names)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Age)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Location)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Phone_number)
                    </th>
                    @*<th>
                            @Html.DisplayNameFor(model => model.Date)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Condition)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.T_History)
                        </th>*@
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Full_names)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Age)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Location)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Phone_number)
                    </td>
                    @*<td>
                            @Html.DisplayFor(modelItem => item.Date)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Condition)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.T_History)
                        </td>*@
                    <td>
                        <a asp-action="Edit" class="btn btn-outline-dark btn-sm" asp-route-id="@item.ID">Edit</a> |
                        <a asp-action="Details" class="btn btn-outline-dark btn-sm" asp-route-id="@item.ID">Details</a> |
                        <a asp-action="Delete" class="btn btn-outline-dark btn-sm" asp-route-id="@item.ID">Delete</a>
                    </td>
                </tr>
}
            </tbody>
        </table>
    </div>
</div>